{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build & test",
            "command": "pipeline configure_cmake && pipeline execute_build && pipeline run_unit_tests"
        }
    ]
}