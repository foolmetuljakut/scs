{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "pipeline configure_cmake && pipeline execute_build"
        }
    ]
}